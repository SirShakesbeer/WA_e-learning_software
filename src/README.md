# WorkAdventure Map Starter Kit - Src Folder

In this directory you can put your scripts and other source code files.

Chat trigger locations müssen in der map (z.B. in Tiled) als Objekt mit Namen "{name}" und Klasse "area" erstellt werden. Die Namen müssen im areaManager in das Array chatZones eingefügt werden, damit sie gefunden werden können.